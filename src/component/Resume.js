import PdfFile from "../cv.pdf"
const Resume=()=>{
        return (
            <div className="resume">
                <h1> <a href={PdfFile}>View Full Resume</a></h1>
            </div>
        )
    }

export default Resume;    